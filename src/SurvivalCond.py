from enum import Enum
from enum import auto

class SurvivalConditions(Enum):
    LeftandRight = auto()
    Left = auto()
    Right = auto()
    Top = auto()
    Bottom = auto()
    TopandBottom = auto()
    Center = auto()

