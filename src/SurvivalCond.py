from enum import Enum
from enum import auto

class SurvivalConditions(Enum):
    LeftandRight = auto()
    Left = auto()
    Right = auto()
    Center = auto()
    Top = auto()
    Bottom = auto()
    TopandBottom = auto()
